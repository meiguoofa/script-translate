1. 现在使用MPS我们自定义的模板 保留原视频所有参数不变的模板 进行字幕烧录到视频中, 默认使用MPS这种烧录方式
2. 字幕翻译默认使用下面的API，API-Key是sk-j3AeRdRwkIWs37xxGAQmkhOCA4TTDYh0WSyCTXKpIN9DiOIz, 字幕翻译QPS调到30
API文档是:OpenAI 兼容 API
Mobinova 平台完全兼容 OpenAI API 格式。如果您已经使用过 OpenAI 的 API，只需将 base_url 替换为本站地址即可。

Base URL

https://mobinova.cc/v1
认证
所有 API 请求都需要在 HTTP Header 中携带 API 密钥：


Authorization: Bearer sk-your-api-key-here
Chat Completions
请求地址

POST https://mobinova.cc/v1/chat/completions
请求参数
参数	类型	必填	说明
model	string	✅	模型 ID，参见 模型列表
messages	array	✅	消息列表
temperature	float	❌	采样温度，0~2，默认 1
top_p	float	❌	核采样参数，0~1
max_tokens	integer	❌	最大生成 Token 数
stream	boolean	❌	是否流式输出，默认 false
stop	string/array	❌	停止生成的标记
消息格式
json

{
  "messages": [
    {"role": "system", "content": "你是一个有帮助的助手"},
    {"role": "user", "content": "你好"},
    {"role": "assistant", "content": "你好！有什么我可以帮助你的吗？"},
    {"role": "user", "content": "帮我写一段 Python 代码"}
  ]
}
支持的角色类型：

system：系统提示词，设定助手行为
user：用户消息
assistant：助手回复（用于多轮对话历史）
请求示例
非流式请求
bash

curl https://mobinova.cc/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-your-api-key" \
  -d '{
    "model": "gpt-4o",
    "messages": [
      {"role": "user", "content": "用一句话介绍 Python"}
    ]
  }'
响应：

json

{
  "id": "chatcmpl-xxx",
  "object": "chat.completion",
  "model": "gpt-4o",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "Python 是一种简洁优雅、易于学习且功能强大的高级编程语言。"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 12,
    "completion_tokens": 18,
    "total_tokens": 30
  }
}
流式请求
bash

curl https://mobinova.cc/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-your-api-key" \
  -d '{
    "model": "gpt-4o",
    "messages": [
      {"role": "user", "content": "用一句话介绍 Python"}
    ],
    "stream": true
  }'
响应（SSE 格式）：


data: {"id":"chatcmpl-xxx","choices":[{"delta":{"content":"Python"},"finish_reason":null}]}

data: {"id":"chatcmpl-xxx","choices":[{"delta":{"content":" 是一种"},"finish_reason":null}]}

data: [DONE]
Models API
查看可用模型：

bash

curl https://mobinova.cc/v1/models \
  -H "Authorization: Bearer sk-your-api-key"
兼容性说明
功能	支持状态
Chat Completions	✅ 完全支持
流式输出（SSE）	✅ 完全支持
多轮对话	✅ 完全支持
System Prompt	✅ 完全支持
函数调用（Function Calling）	⚠️ 部分模型支持
JSON Mode	⚠️ 部分模型支持
Vision（图片理解）	⚠️ 部分模型支持
Embeddings	⚠️ 部分模型支持
Fine-tuning	❌ 不支持

4. 通过优化并发任务 增加QPS 优化  当前字母擦除 + 翻译 + 烧录的整体效率 


